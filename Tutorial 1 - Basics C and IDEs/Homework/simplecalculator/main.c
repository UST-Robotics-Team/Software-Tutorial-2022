#include <stdio.h>
#include <stdlib.h>

char* msg_input = "Input:\n";
char* msg_output = "Output:\n";
int main(){
    printf(msg_input);
    /**
     * @brief 
     * get input from the terminal, please refer to the Problem_Set.md for format.
     * Then do the calculation according to the input.(Only simple math operation is needed, i.e. Addition, Subtraction, Multiplication and Division.)
     * Example output to been seen in the terminal:
     * Input:
     * 2
     * +
     * 3
     * Output:
     * 5
     */
    // TODO:
    printf(msg_output);
    return 0;
}